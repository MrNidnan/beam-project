## FIX

- Preserve the ratio of CoverArt, don't make them all square

## FEATURES

- Add an option to use the original %Singer tag from JRiver instead of calculating it.
- Import export settings. Add an option to define the storage place for the settings and options. It would facilitate transferring the settings to several computers
- Add an option to define a uniform color for the background instead of a picture. It would save rendering?
- I would use the Artist Background feature if it allowed to define the cover art tag as background picture.

- I would like to be able to disable default mood so I can configure different Defaults.
  Right now when I disable the default still is loaded in seetings and settings. Maybe it is a limitation because we always need a default mood.
  In that case we shuold be able to transfer the default mood to another one.
  ! This is more a revamp change of core funciotnality moving awaing from moods to display items....
